// Tier-3 game-specific chart — STUB placeholder (Part 3 builds the real
// price-distribution chart in the hand-rolled-SVG house style). This tile EXISTS
// and is wired into the shared ReportBoard now; only its content is deferred.

export function StubChart() {
  return (
    <div
      style={{
        display: 'flex', alignItems: 'center', justifyContent: 'center',
        height: 120, border: '1px dashed #cbd5e1', borderRadius: 6,
        background: '#f8fafc', color: '#94a3b8', fontSize: '0.8rem',
        textAlign: 'center', padding: '0.5rem',
      }}
    >
      Price-distribution chart — coming in Part 3
    </div>
  )
}
